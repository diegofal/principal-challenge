import type { NextWebVitalsMetric } from 'next/app';

/**
 * 🚧 Placeholder for custom web-vitals reporting.
 * TODO (candidate): send CLS/LCP/FID metrics to `/api/metrics` or external analytics.
 */
export function reportWebVitals(_metric: NextWebVitalsMetric) {
  // Intentionally empty.
} 